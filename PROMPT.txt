```md
You are implementing the documentation toolchain for:

Repository:
https://github.com/obinexus/obi

Project:
OBI — Ontological Bayesian Intelligence

Author:
Nnamdi Michael Okpala / OBINexus

Current repo structure includes:

.
├── .circleci/
├── docs/
├── nw/
├── obi/
├── scripts/
├── www/
├── .gitignore
├── package.json
├── pyproject.toml
└── README.md

Main goal:
Build a documentation system where LaTeX, MiKTeX, BibTeX, Markdown, and plain text documentation can be converted into clean, consistent, indexable HTML and served locally or published online.

The OBI documentation must be transferable from:

- .tex
- .bib
- .md
- .txt
- generated PDF references where available

into:

- static HTML
- searchable JSON index
- consistent UI layout
- local web server
- NW.js / node-webkit desktop documentation app
- GitHub Pages / CircleCI deployable artifact

Important concept:
OBI means Ontological Bayesian Intelligence. It is a semantic reasoning framework under uncertainty using ontologies, Bayesian inference, stochastic models, semantic graphs, top-down reasoning, bottom-up reasoning, filter/flash reasoning cycles, and robotics-interface infrastructure.

Important wording rule:
Do NOT describe OBI as conscious AI.
Describe OBI as:
“an ontological and Bayesian reasoning infrastructure for uncertainty-aware robotic and humanitarian systems.”

The source documentation explains OBI in terms of semantic graphs, probabilistic reasoning, uncertainty, stochastic subject matter, top-down/bottom-up reasoning, semantic drift, data drift, filter/flash reasoning, robotics interfaces, and humanitarian real-world assistance systems.

Required implementation:

1. Documentation source layout

Create or enforce this structure:

docs/
├── source/
│   ├── tex/
│   ├── bib/
│   ├── md/
│   ├── txt/
│   └── assets/
├── public/
│   ├── index.html
│   ├── search.json
│   ├── bibliography.html
│   ├── papers.html
│   ├── manifest.json
│   ├── robots.txt
│   ├── sitemap.xml
│   └── assets/
│       ├── obi.css
│       └── obi.js
└── archive/

Move documentation into the correct source folders without deleting anything.

Examples:
- .tex files go into docs/source/tex/
- .bib files go into docs/source/bib/
- .md files go into docs/source/md/
- .txt files go into docs/source/txt/
- images and diagrams go into docs/source/assets/
- unclear or old research ZIPs go into docs/archive/

Do not destroy original files.
Do not rename aggressively unless safe.
If renaming, use kebab-case and maintain an index mapping old name to new name.

2. Node.js documentation build system

Implement Node.js scripts in scripts/:

scripts/
├── clean-docs.mjs
├── build-docs.mjs
├── serve-docs.mjs
├── convert-tex.mjs
├── convert-bib.mjs
├── index-docs.mjs
└── validate-docs.mjs

The build pipeline should:

- clean previous generated HTML safely
- scan docs/source/
- convert .txt into structured HTML sections
- convert .md into HTML
- convert .tex into HTML
- parse .bib into bibliography HTML and JSON
- generate search.json
- generate sitemap.xml
- generate robots.txt
- generate manifest.json
- copy assets
- validate that required output files exist

3. LaTeX / MiKTeX / BibTeX support

The system must support LaTeX documentation written for MiKTeX.

Important:
MiKTeX itself may be installed locally on Windows, but the Node.js build should not require MiKTeX just to generate basic HTML.

Use a fallback approach:

Primary HTML path:
- Parse .tex directly using a lightweight Node-compatible approach.
- Convert common LaTeX structures to semantic HTML.

Optional PDF path:
- If pdflatex/xelatex/bibtex are available, support building PDF outputs.
- If they are not installed, skip PDF generation gracefully.

Supported LaTeX features:

- \title{}
- \author{}
- \date{}
- \maketitle
- \begin{abstract}...\end{abstract}
- \section{}
- \subsection{}
- \subsubsection{}
- \paragraph{}
- \textbf{}
- \textit{}
- \emph{}
- \cite{}
- \citep{}
- \citet{}
- \ref{}
- \label{}
- \begin{itemize}
- \begin{enumerate}
- \item
- \begin{equation}
- \begin{align}
- inline math using \( ... \)
- display math using \[ ... \]
- bibliography references through .bib files

For math:
- Preserve TeX math syntax.
- Render math in HTML using MathJax.
- Include MathJax in generated HTML.

For BibTeX:
- Read .bib files from docs/source/bib/
- Generate docs/public/bibliography.html
- Generate docs/public/bibliography.json
- Resolve citations where possible.
- If a citation key cannot be found, show it clearly as unresolved but do not fail the build.

4. HTML output rules

Every generated HTML page must use the same layout.

Create a shared layout function/template containing:

- document title
- OBINexus / OBI brand header
- left/table-of-contents navigation
- main content area
- search box
- footer
- canonical URL placeholder
- OpenGraph metadata
- Twitter card metadata
- JSON-LD schema.org metadata
- responsive layout
- accessible skip-to-content link

Use semantic HTML:

<header>
<nav>
<main>
<article>
<section>
<footer>

Do not generate raw transcript dumps.

5. UI consistency requirements

Create one global CSS file:

docs/public/assets/obi.css

The UI must be consistent across all generated pages.

Use a clean technical documentation style:

- dark header
- light content area
- readable typography
- consistent spacing
- consistent code blocks
- consistent mathematical notation display
- responsive mobile layout
- accessible contrast
- obvious links
- sticky or visible table of contents where reasonable

Design tokens should exist at the top of CSS:

:root {
  --obi-bg: ...
  --obi-surface: ...
  --obi-text: ...
  --obi-muted: ...
  --obi-accent: ...
  --obi-border: ...
  --obi-code-bg: ...
  --obi-radius: ...
  --obi-max-width: ...
}

Do not use many random styles per page.
Everything should feel like one documentation system.

6. JavaScript interface

Create:

docs/public/assets/obi.js

It should provide:

- client-side search using search.json
- keyboard-accessible search results
- active table-of-contents highlighting
- copy buttons for code blocks
- mobile navigation toggle if needed
- no heavy frontend framework

Do not use React.
Do not use Next.js.
Do not use Vite unless absolutely necessary.
Keep it static and simple.

7. package.json

Implement package.json with scripts:

{
  "scripts": {
    "clean": "node scripts/clean-docs.mjs",
    "docs:build": "node scripts/build-docs.mjs",
    "docs:serve": "node scripts/serve-docs.mjs",
    "docs:index": "node scripts/index-docs.mjs",
    "docs:validate": "node scripts/validate-docs.mjs",
    "publish:check": "npm run docs:build && npm run docs:validate",
    "nw:start": "nw nw",
    "nw:build": "echo \"NW.js packaging placeholder\""
  }
}

Dependencies should be minimal.

Allowed dependencies:
- fast-glob
- gray-matter
- marked
- bibtex-parse-js
- slugify
- chokidar
- http-server or a custom Node server

Prefer custom Node scripts if simple.

8. pyproject.toml

Keep the Python project metadata.

Also add optional Python documentation tooling compatibility.

The project should remain installable with:

pip install -e .

Do not make Python mandatory for the Node docs build.
Python may be used later for advanced indexing, NLP cleanup, or research tooling.

9. NW.js / node-webkit support

Implement:

nw/package.json

The NW.js app must launch:

../docs/public/index.html

Use:

- app name: OBI Documentation
- window title: OBI — Ontological Bayesian Intelligence
- width: 1200
- height: 800

The user calls this “node-webkit”, but implement using modern NW.js naming.

10. CircleCI

Implement:

.circleci/config.yml

Pipeline:

- checkout
- use Node LTS
- npm install
- npm run clean
- npm run docs:build
- npm run docs:validate
- store docs/public as artifact

11. GitHub publish workflow

Also create:

.github/workflows/publish.yml

It should:

- run on push to main
- install dependencies
- build docs
- validate docs
- upload docs/public as GitHub Pages artifact
- deploy to GitHub Pages

12. Documentation pages to generate

Generate at least:

docs/public/index.html
docs/public/papers.html
docs/public/bibliography.html
docs/public/search.json
docs/public/sitemap.xml
docs/public/robots.txt
docs/public/manifest.json

The main index page should have these sections:

- What is OBI?
- Why uncertainty matters
- Ontologies as semantic graphs
- Bayesian reasoning
- Stochastic subject matter
- Top-down and bottom-up reasoning
- Semantic drift and data drift
- Filter/flash reasoning cycle
- OBI for robotics
- Humanitarian real-world systems
- What OBI is not
- Glossary
- Source documents

13. Source document preservation

Create:

docs/source/README.md

It should explain:

- where to place .tex files
- where to place .bib files
- where to place .md files
- where to place .txt files
- how to build docs
- how to serve docs
- how to add a new paper
- how citations are handled
- how MathJax is handled

14. README.md

Update root README.md with:

- project overview
- OBI definition
- what OBI is not
- repository layout
- install instructions
- Node.js build instructions
- MiKTeX / LaTeX notes
- BibTeX notes
- NW.js / node-webkit notes
- CircleCI notes
- GitHub Pages publishing notes

15. Acceptance criteria

These commands must work:

npm install
npm run clean
npm run docs:build
npm run docs:serve
npm run docs:validate
npm run publish:check

These files must exist after build:

pyproject.toml
package.json
README.md
.circleci/config.yml
.github/workflows/publish.yml
nw/package.json
docs/source/README.md
docs/public/index.html
docs/public/papers.html
docs/public/bibliography.html
docs/public/search.json
docs/public/sitemap.xml
docs/public/robots.txt
docs/public/manifest.json
docs/public/assets/obi.css
docs/public/assets/obi.js

16. Quality bar

The documentation must be:

- searchable
- indexable
- accessible
- mobile responsive
- consistent across all pages
- compatible with static hosting
- compatible with NW.js desktop viewing
- respectful of original source files
- clear about OBI as a reasoning infrastructure, not conscious AI

Do not overengineer.
Do not introduce a complex web framework.
Use static HTML generation with Node.js.
Keep the system easy for Nnamdi to maintain from PowerShell.
```